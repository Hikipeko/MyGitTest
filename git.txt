1. never commit or push on master!
2. each group member holds a branch, create new branch for feature, bugs, etc.
3. let other group members check before mergeing with master
4. if conflict when merging, pull -> resolve conflict -> merge